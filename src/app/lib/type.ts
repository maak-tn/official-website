export type TNavItems = {
    name: string;
    link: string;
};

export type TStatsCard = {
    label: string;
    description: string;
}

export type TProcessItem = {
    title: string;
    duration: string;
    description: string;
    customList: string[];
};